---
export type Props = Record<string, unknown>

const {identity, ...props} = Astro.props;
---
<script is:inline type="application/json" id=`context-${identity}` set:html={JSON.stringify(props, null, 2)}></script>